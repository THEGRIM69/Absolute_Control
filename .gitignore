# ignorar binarios compilados de Java
*.class

# ignorar configuraciones del editor
.vscode/

# Ignorar arcvhivos empaquetados y librerias binarias
*.jar
*.war
*.ear

# Ignorar librerias nativas del sistema
*.so
*.dll
*.dylib

!kvm-client/lib/*.jar
!kvm-client/lib/*.so

# Ignorar los compilados de Java
**/out/
**/*.class

# Ignorar la carpeta de build completa (generada por build.ps1): incluye
# build/classes (compilados intermedios), build/AbsoluteControl.jar, y
# build/installer (el .exe final + el checksum .sha256.txt). Estos NO se
# versionan en el repo — se publican como adjuntos en GitHub Releases.
build/

*.pfx